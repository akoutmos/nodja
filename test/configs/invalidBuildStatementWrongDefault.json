{
    "variables": {
        "var0": "val0"
    },

    "rules": {
        "rule0": "command0"
    },

    "build_statements": [
        {
            "input": "valid",
            "output": "valid",
            "rule": "valid",
            "default": 0
        }
    ]
}
